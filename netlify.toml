# NOTE: This site has migrated to Cloudflare Pages.
# Cloudflare ignores this file. It is kept only so the legacy
# Netlify deployment (if ever reactivated) still builds as a
# plain static site. The Feishu sync now runs via Cloudflare
# Functions (functions/products.js) at request time — no build
# command, no per-deploy quota cost.

[build]
  publish = "."
